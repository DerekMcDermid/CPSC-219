package Project;

import java.util.Random;
import java.util.Scanner;

public class Main {
	public static void main(String[] args) {
		//getting input from user
		Scanner playerInput = new Scanner(System.in);
		Random rand = new Random();
		// setting up random enemies in buildings
		String zombies = ("Zombie");
		int maxZombieHealth = 100;
		int zombieAttackDamage = 25; 
		
		//character
		int health = 100;
		int playerAttackDamage = 50; 
		int startNumBandaiges = 3;
		int healthBandaidAmount = 25;
		int itemDropChance = 25; //25% chance for zombie to drop an item
		
		boolean running = true; 
		
		System.out.println("Welcome to the community of Wolfden!");
		
		GAME:
		while(running) {
			System.out.println("----------------------------------------------");
			
			int zombieHealth = rand.nextInt(maxZombieHealth);
			String 
			
		}
		
	}
}
